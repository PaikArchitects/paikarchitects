# 그리드 하단 빈칸 수정 (후속 패치) — 개수 무관 일반화

> 대상: Claude Code. `GridExperience.tsx`만 수정. 다른 파일 손대지 말 것.

## 전제 정정
- 퍼블리싱 프로젝트 수는 **가변**(현재 26개, 변동 가능). 특정 개수(30)를 하드코딩하지 말 것.
- 따라서 "마지막 행이 덜 차는 것"은 **개수가 열 수의 배수가 아닐 때 정상**이다.
  - 예: 26개 · 5열 → 6행, 마지막 행 1칸(25+1). 26개 · 6열 → 5행, 마지막 행 2칸(24+2). 둘 다 정상.
- **진짜 버그**: 마지막 행이 "나머지(total % N)"보다 **더 적게** 채워지는 경우, 또는 중간 행에 빈 슬롯이 생기는 경우, 또는 일부 카드가 아예 배치 누락되는 경우.

## 원인 (확인 후 수정)
정수 열 상태(비전환)에서도 전환용 "대기 카드/새 열 슬롯" 로직이 잔존하여 일부 카드가 배치 누락. 정수 상태에서는 total개를 i순으로 N열 격자에 빈틈없이 배치해야 함.

## 수정 요구
1. **정수 열 상태(t=0)**: visibleCards 전체(total = published 카운트, 필터 시 해당+비해당 전부)를 index 순으로 N열 격자에 배치. `row=floor(i/N)`, `col=i%N`. **total개 전부 배치, 누락 0.**
2. **stage 높이**: `rows = ceil(total/N)`, `height = rows*rowH`. total은 하드코딩 아니라 실제 카드 수.
3. **전환 로직 분리**: 대기 카드를 새 열에 끌어오는 로직은 **전환 중(0<t<1)에만**. 정수 정착 후 순수 i순 배치로 리셋.
4. **검증(개수 무관)**: 임의 N에서 배치된 카드 수 == total, 누락 0. 마지막 행 채워진 칸 수 == (total % N == 0 ? N : total % N). 중간 행 빈 슬롯 0.

## 실행 프롬프트
```
GridExperience.tsx 에서 그리드 하단에 카드가 누락되어 빈칸이 생기는 버그를 고쳐줘.
- 퍼블리싱 개수는 가변이다(현재 26). 30 등 특정 수를 하드코딩하지 마.
- 정수 열 상태(전환 아닐 때)는 전체 카드(필터 시 해당+비해당 전부)를 index 순으로 N열 격자에 빈틈없이 배치. row=floor(i/N), col=i%N. total개 전부 배치, 누락 0.
- 전환용 대기카드/새열슬롯 로직은 전환 중(0<t<1)에만 작동, 정수 정착 후 순수 i순 배치로 리셋.
- stage 높이 = ceil(total/N)*rowH. total은 실제 카드 수.
- 검증: 임의 N에서 배치 카드수==total, 누락 0, 중간행 빈슬롯 0. 마지막 행이 나머지(total%N)만큼만 차는 건 정상.
- 다른 파일 수정 금지. 완료 후 npx tsc --noEmit만.
```
