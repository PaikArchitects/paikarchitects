# 그리드 모드 명세 v3 — 동일 크기 유지 전환

> 대상: Claude Code. 기존 `/work-grid`를 전면 교체. 링월(`/work`)·랜딩(`/`)·ContentArea 등 기존 파일 **절대 수정 금지**. 신규 2파일(`src/app/work-grid/page.tsx`, `src/components/GridExperience.tsx`)만. 콘텐츠 morph는 범위 밖.

> v2 대비 변경: 밀도 전환 모델을 **"모든 카드 항상 동일 크기 + 화면 밖 대기"** 로 재정의. 호버 간격·6열 빈 레이아웃·필터 방식 수정.

---

## 0. v2 구현에서 확인된 결함 (이번에 고침)

1. **전환 중 카드 크기가 서로 다름** — 새로 생기는 열의 카드가 작게 시작해 커지고, 기존 카드는 작아짐. 원인: 카드 폭을 "현재 분수 열 c"에 맞춰 전체 수렴시켜, 새 카드가 중간 크기로 나타남. → **모든 카드는 매 순간 서로 동일 크기여야 한다.**
2. **호버 요약 텍스트가 타이틀에서 너무 아래로 떨어짐.**
3. **6열에서 레이아웃 대부분이 빈칸** — 30개가 다 배치되지 않음.
4. **필터가 비해당 카드를 숨겨(display:none) 큰 빈 공간 발생** — dim 재정렬로 바꿔야 함.

---

## 1. 밀도 전환 — 동일 크기 유지 모델 (핵심)

### 1-1. 확정 거동 (사용자 언어)

"모든 카드는 같은 크기·같은 간격. 새 카드는 그 행의 화면 밖에 **이미 목표 크기로 존재**했는데 안 보이고 있다가, 열이 늘면 화면 안으로 들어오며 opacity로 드러난다. 크기가 서로 다른 카드는 전환 중에도 없다."

### 1-2. 좌표 모델

전환을 **정수 열 A → 정수 열 B** 사이 보간으로 다룬다 (예 A=3, B=4). 진행도 `t` (0=A, 1=B).

- **카드 크기(공통)**: 전환 중 모든 카드가 동일 크기. 크기는 A크기와 B크기 사이를 t로 보간:
  - `cardW(t) = lerp(cardW_A, cardW_B, t)`, `cardH = cardW / (4/3)`.
  - `cardW_A = (contentW - gap*(A-1))/A`, `cardW_B = (contentW - gap*(B-1))/B`.
  - **모든 카드가 이 단일 cardW(t)를 공유.** 새 카드도 예외 없이 같은 값. (v2 버그: 새 카드만 다른 크기였음 → 금지)
- **열 수(레이아웃 격자)**: 전환 중에는 **B열 격자**로 배치한다(더 많은 쪽). 즉 t>0이면 이미 B개 열의 격자로 카드를 놓는다.
  - 카드 i의 위치: `col = i % B`, `row = floor(i / B)`.
  - `x = col * (cardW(t) + gap)`, `y = row * rowH(t)`.
  - **주의**: 이 `i % B`는 v2에서 금지했던 것과 다르다. v2 문제는 "전환 순간 A→B로 col이 급변해 카드가 순간이동"이었다. 여기서는 t=0(A상태)에서도 **격자는 B로 두되, B번째 열(col=A..B-1)의 카드를 화면 밖 + opacity 0**으로 둔다. 따라서 A상태에서 눈에 보이는 것은 정확히 A열이고, col=0..A-1 카드는 A상태에서 이미 제 위치. t가 커져도 이들의 col은 변하지 않는다(0..A-1 그대로). **기존 카드는 col 불변 → 위치 이동은 크기 축소에 따른 x 조정뿐, 행 이동 없음.**
- **화면 밖 대기 + opacity**:
  - col ≥ A 인 카드(=새로 생기는 열): t=0일 때 `x`는 화면 우측 경계 밖(cardW_A 기준으로 계산하면 자연히 밖). opacity=0.
  - t가 커지며 전체 cardW가 줄어들면 이 카드의 x가 화면 안으로 들어옴. opacity는 t에 연동 0→1 (약간 지연: `clamp((t-0.15)/0.7, 0, 1)`).
  - **핵심**: 새 카드는 "이동해 들어오는" 게 아니라, 원래 그 col 자리에 목표 크기로 있었고 전체 축소로 화면이 그 자리를 품게 됨. 크기는 항상 공통 cardW(t).
- **좌우 오버플로 차단**: stage에 `overflow-x: hidden` (또는 body). 화면 밖 카드는 잘려 안 보이되 가로 스크롤바 없음.

### 1-3. 기존 카드 위치 고정의 정확한 의미

- col 0..A-1 카드: 전환 내내 자기 col 유지. row도 유지(전환 중 격자가 B열이어도, 이 카드들의 i는 그대로라 row=floor(i/B)가 바뀔 수 있음 → **이게 문제**).
- **문제 해결**: row 이동을 막으려면, 전환 중 카드의 (row,col) 배정을 **A격자와 B격자 중 "보이는 카드는 A 유지"** 하도록 매핑해야 한다. 즉:
  - 보이는 카드(원래 A에서 배치된 것)는 A의 (row,col)을 유지.
  - B에서 새로 생기는 슬롯(각 행의 A번째 열, 즉 col=A..B-1)에만 **대기 카드**(아직 안 보이던 뒤쪽 카드)를 넣는다.
  - 예 A=3→B=4: 1행=[card0,1,2 + 대기card X], 2행=[card3,4,5 + 대기card Y]... 기존 card0~5는 행·열 불변, 새 열에만 X,Y 페이드인.
  - 이 방식이 "행 이동 없음"을 보장한다. (v2 명세 §2-3의 슬롯 매핑과 동일 원리 — 반드시 이대로.)
- **순서 자유**: 대기 카드 X,Y는 projectOrder에서 아직 안 쓴 다음 카드. careerNo 순서 안 지켜도 됨(사용자 확정).

### 1-4. 드래그/릴리스

- 드래그 중 t 연속. 릴리스 시 가장 가까운 정수로 rAF 트윈(420ms easeOutCubic).
- 정수 정착 후 다음 전환은 A,B 재설정(예 4 정착 → 다음은 A=4,B=5 또는 A=3,B=4 방향에 따라).

### 1-5. 절대좌표 구현

- position:absolute + `transform: translate(Xpx, Ypx)`, px 기반. 퍼센트 금지(Safari).
- width/height/opacity를 매 프레임 세팅.

---

## 2. 카드 4:3 균등폭 (v2 §1 유지)

- 항상 4:3 프레임, object-fit:cover, coverHotspot으로 object-position. 원본 aspectRatio를 폭에 반영 금지.

---

## 3. 6열 전 카드 배치 (빈 레이아웃 수정)

- 어느 정수 열 N에서도 **30개 전부 배치**. 행 수 = ceil(30/N). 6열이면 5행(30개 꽉).
- v2 빈칸 원인은 전환 모델의 카드 크기·슬롯 계산 오류였음 → §1 모델로 각 열 수에서 30개가 정확히 격자를 채우도록.
- 정수 상태(비전환)에서는 단순 N열 격자에 i순으로 30개 배치. 빈 슬롯 없음.

---

## 4. 필터 — dim 재정렬 (숨김 금지)

- 이전: 비해당 카드 display:none → 큰 빈 공간. **폐기.**
- 신규: **해당 카드를 좌상단부터 빈틈없이 앞쪽에 배치**(opacity 1), **비해당 카드는 그 뒤에 이어서 배치**(opacity 낮춤, 예 0.15).
- 전체 30개가 항상 화면에 존재. 필터는 **순서 재정렬 + opacity 차등**일 뿐 카드 제거 아님.
- 구현: projectOrder를 [해당 카드들 ..., 비해당 카드들 ...] 순으로 재정렬 후 layout 재실행. 비해당은 `.dim` 클래스(opacity:0.15). 재정렬 시 부드러운 transform 트랜지션.
- 필터 대상이 소수여도 빈칸 없이 그리드가 꽉 참(비해당이 뒤를 채우므로).

---

## 5. 호버 텍스트 간격 (수정)

- v2: 요약이 타이틀에서 너무 아래. → 타이틀 **바로 아래**에 붙도록 간격 축소.
- `card-text` 예약 높이 축소, 요약의 상단 여백 최소화(타이틀과 4~6px 간격). 호버 시 예약 공간에 페이드인, reflow 없음.
- 타이틀 상시 노출, 요약(타이폴로지·AWARDS) 호버 시. 전 구간 노출.

---

## 6. 그 외 (유지)

- 1열 폭 = (4/3)*(vpH*0.72) 히어로폭 중앙정렬. MIN_COLS 한 곳 정의.
- 종횡비 상한: r<0.85→2, <1.25→4, ≥1.25→6.
- 덴시티바 width:min(440px,64vw). 스냅 아이콘은 트랙과 같은 좌표계(트랙폭 기준 colsToPos)로 트랙 안에 정렬. 바 늘리지 말 것. 박스만(숫자 없음).
- 카드 cursor:pointer.

---

## 7. 검증 (좌표 측정, 영상 판독 불필요)

- `npx tsc --noEmit`만.
- 신규 2파일 외 diff 없음.
- **동일 크기 검증**: 전환 중(t=0.5) 모든 카드(새 카드 포함) width가 1px 오차 내 동일.
- **위치 고정 검증**: 전환 중 기존 카드의 (row,col)이 A와 동일, 행 이동 없음.
- **6열 채움 검증**: 6열에서 카드 30개 전부 배치, 5행 꽉.
- **필터 검증**: 필터 시 해당 카드 앞쪽 opacity 1, 비해당 뒤쪽 opacity 0.15, 빈칸 없음.
- **슬라이더 정렬**: 첫/마지막 스냅 아이콘 = 트랙 좌/우 끝.

---

## 실행 프롬프트

```
GRID_MODE_V3_SPEC.md 파일을 읽고 명세대로 구현해줘. 기존 /work-grid 전면 교체.

절대 제약:
- 기존 파일(page.tsx, work/page.tsx, LandingExperience/ContentArea/ProjectWall/MobileProjectWall.tsx, useRingWall.ts) 수정 금지. 신규 2파일만.

밀도 전환 핵심(v2 버그 수정):
- 전환 중 모든 카드는 항상 서로 동일 크기(공통 cardW(t)). 새 카드만 작게 시작하는 버그 금지.
- 카드 크기는 정수열 A크기→B크기를 t로 보간한 단일 값을 전 카드 공유.
- 새로 생기는 열 카드는 그 행의 col=A..B-1 자리에 목표크기로 이미 존재하되, t=0에서 화면 밖(x가 화면 우측 밖)+opacity0. t 커지며 전체 축소로 화면 안에 들어오고 opacity 0→1(지연 clamp((t-0.15)/0.7)).
- 기존 보이던 카드는 A의 (row,col) 유지, 행 이동 절대 금지. 새 열 슬롯만 대기카드로 채움(순서 자유).
- overflow-x:hidden으로 가로스크롤 차단. 절대좌표 transform translate(px, 퍼센트 금지).

기타 수정:
- 6열 포함 모든 정수열에서 30개 전부 배치, 빈칸 없음(6열=5행 꽉).
- 필터: 비해당 카드 display:none 하지 말고, 해당 카드를 좌상단부터 앞에 opacity1로, 비해당은 뒤에 opacity0.15로 재정렬. 빈칸 없이 그리드 꽉 채움.
- 호버 요약을 타이틀 바로 아래(4~6px)에 붙임. 지금은 너무 아래.
- 슬라이더 스냅 아이콘을 트랙과 같은 폭·좌표계로 트랙 안에 정렬. 바 늘리지 마. 덴시티바 width:min(440px,64vw).
- 카드 cursor:pointer. 1열 폭 (4/3)*(vpH*0.72) 중앙정렬. 종횡비 상한 2/4/6.
- 완료 후 npx tsc --noEmit만.
```
