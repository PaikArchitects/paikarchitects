# 그리드 모드 재설계 명세 v2 — plomp식 위치 고정 전환

> 대상: Claude Code. 기존 `/work-grid` 구현을 **전면 교체**한다. 핵심은 밀도 전환 애니메이션의 재설계다. 링월(`/work`)·랜딩(`/`)·`ContentArea.tsx` 등 기존 파일은 **절대 수정 금지**(1차 명세 §0과 동일). 신규 파일 `src/app/work-grid/page.tsx`, `src/components/GridExperience.tsx`만 다룬다. 콘텐츠 morph는 이 명세 범위 밖(추후 별도).

---

## 0. 이번 재설계가 고치는 것 (1차 구현의 실패)

1차 구현에서 확인된 근본 결함 두 가지:
- **카드가 4:3 균등폭이 아니라 원본 비율 폭으로 렌더됨** → 같은 열에서 폭 제각각, 스크롤바, 필터 깨짐.
- **밀도 전환 시 카드가 자리를 이동함** → 3→4열 시 4번 카드가 2행에서 1행으로 올라오고, 그 자리를 5번이 채우는 연쇄 이동 발생. 이는 `col = i % ncol` 방식이 열 수 변경 순간 전체 카드를 재배치하기 때문.

이 명세의 핵심 요구: **화면에 보이는 카드는 밀도 전환 중 절대 위치를 바꾸지 않는다.**

---

## 1. 카드 4:3 균등폭 (절대 원칙)

- 모든 카드는 **동일 폭**. 폭은 열 수에서 계산: `cardW = (contentW - gap*(cols-1)) / cols`.
- 카드 이미지 프레임은 **4:3 고정**: `aspect-ratio: 4/3`, `object-fit: cover`.
- **원본 이미지 비율(queries.ts의 aspectRatio)을 카드 폭에 반영하지 말 것.** 원본이 16:9든 A판형이든 카드 프레임은 항상 4:3이고, 이미지는 cover로 채운다.
- 16:9 등 크롭 시 `object-position`을 `coverHotspot`(Project.coverHotspot {x,y}, queries.ts 공급)으로 지정. 없으면 center.
- 검증: 임의 열 수에서 모든 카드 `clientWidth`가 1px 오차 내 동일해야 한다.

---

## 2. 밀도 전환 — plomp식 위치 고정 모델 (이 명세의 핵심)

### 2-1. 확정된 거동 (사용자 관찰로 확정)

3열 → 4열 전환 시:
1. 4번째 열 **슬롯이 폭 0에서 연속 확대**되며 열린다.
2. 동시에 기존 3개 열 카드의 폭이 연속 축소, 전체가 4열 최종 크기로 **연속 수렴**.
3. 새로 열리는 4번째 열의 각 슬롯은 **화면에 아직 없던 대기 카드**로 채우고 **opacity 0→1 페이드인**.
4. **기존에 보이던 카드는 자기 (행,열) 자리를 절대 이동하지 않는다.** 폭만 줄어든다.

### 2-2. 왜 `i % ncol`이 금지인가

`col = i % ncol` / `row = floor(i / ncol)`는 `ncol`이 3→4로 바뀌는 순간 **모든 카드의 (행,열)을 재계산**한다. 그 결과 4번 카드가 2행→1행으로 순간이동한다. **이 방식을 쓰지 말 것.**

### 2-3. 올바른 구현 — 슬롯-카드 명시적 매핑

전환을 "정수 열 A → 정수 열 B" 사이의 연속 보간으로 다룬다(A=3, B=4 예시).

- **레이아웃 A(3열)와 레이아웃 B(4열)를 각각 독립적으로 계산**한다. 각 레이아웃은 "슬롯 리스트"다. 슬롯 = {row, col, x, y, w, h, cardId}.
  - 레이아웃 A: 슬롯을 index 순으로 3열 격자에 배치. 슬롯 i → cardId = projectOrder[i].
  - 레이아웃 B: 슬롯을 index 순으로 4열 격자에 배치. 슬롯 i → cardId = projectOrder[i].
  - **projectOrder는 두 레이아웃에서 동일**(같은 카드 순서 리스트). 순서 자유이므로 careerNo 순서를 강제하지 않아도 되나, A와 B가 같은 순서 리스트를 써야 한다.
- **전환 진행도 t (0=A, 1=B)** 에 따라 각 카드의 위치·크기·opacity를 결정:
  - **A·B 양쪽에 존재하고, 두 레이아웃에서 같은 (row,col)인 카드**: 위치 고정, 폭만 A폭→B폭 보간, opacity=1. (이게 "보이던 카드는 이동 안 함"의 핵심 — 대부분의 카드가 여기 해당.)
  - **B에만 새로 생긴 슬롯의 카드**(4번째 열): 위치는 B의 자리 고정, opacity를 t에 연동 0→1 페이드. 폭은 B폭(또는 전체 수렴폭)으로.
  - 주의: A와 B에서 (row,col)이 **다른** 카드가 생기면 이동이 발생한다. 이를 막으려면 **B 레이아웃의 앞부분 (rows×A_cols) 슬롯을 A와 동일 배치로 강제**하고, 4번째 열 슬롯만 뒤쪽 대기 카드로 채운다. 즉:
    - B(4열)에서 1행 = [A의 1행 카드 3개] + [대기카드 X]
    - B(4열)에서 2행 = [A의 2행 카드 3개] + [대기카드 Y]
    - ... 기존 카드는 A에서의 행·열을 그대로 유지, 4번째 열만 신규.
  - **이 방식이 "위치 고정"을 보장한다.** 기존 카드의 col은 A에서든 B에서든 동일(0,1,2), 새 카드만 col=3에 추가.

### 2-4. 역방향(4→3열)

- 4번째 열 카드를 opacity 1→0으로 페이드아웃, 폭은 3열 폭으로 수렴. 기존 카드는 위치 고정.
- 즉 A↔B는 대칭. t를 되감으면 된다.

### 2-5. 드래그 중 / 릴리스

- 드래그 중: t를 슬라이더 위치에서 연속 계산, 매 프레임 layout(t). 정수 열 사이 구간만 보간(예: cols 3.0~4.0 → A=3,B=4,t=cols-3).
- 릴리스: 가장 가까운 정수로 rAF 트윈(420ms, easeOutCubic). 트윈도 동일 layout(t) 사용.
- **정수 상태(t=0 또는 1)에서는 A=B가 되도록** 정규화. 예: cols=4 정착 후 다음 드래그는 A=4,B=5로 재설정.

### 2-6. 대기 카드 풀

- 순서 자유. projectOrder는 초기 1회 정한 순서(careerNo desc 그대로 써도 되고 셔플해도 됨 — 사용자는 순서 무관 확정).
- 열이 늘어 새 슬롯이 필요하면 projectOrder에서 아직 배치 안 된 다음 카드를 꺼내 채운다.
- **총 카드 수(30개)는 고정.** 열 수가 늘면 행이 줄고, 각 행에 카드가 더 들어가므로 전체가 재분배되지만, "보이는 카드 위치 고정" 원칙상 상단부터 순서대로 채운다.

> 구현 주: 이 모델은 절대좌표(position:absolute + transform translate, px 기반) 방식이 자연스럽다. 각 카드 el에 대해 매 프레임 `transform: translate(x,y)`, `width`, `height`, `opacity`를 계산해 세팅. Safari 대비 **transform은 px 좌표만 사용, 퍼센트 금지**.

---

## 3. 1열 특수 처리

- 1열 카드 폭 = **콘텐츠 히어로 폭** `(4/3) × (viewportHeight × 0.72)` (ContentArea 상수 SLIDE_H_RATIO=0.72 참조). 중앙 정렬.
- 2~6열은 §1의 전체폭 균등 분할.
- `MIN_COLS`는 한 곳에서만 정의(실물 판단 후 1→3 변경 대비).
- 1열↔2열 전환도 §2 모델을 따르되, 1열은 폭이 히어로폭으로 별도 수렴.

---

## 4. 밀도 슬라이더 UI (정렬 수정 포함)

- 하단 **전용 바**(fixed), `width: min(440px, 64vw)`로 컴팩트. **바를 더 늘리지 말 것.**
- 구성: `DENSITY`(좌) — 트랙(중, flex:1) — `N cols`(우). 카운트는 DENSITY와 동일 회색·대문자·자간.
- **스냅 아이콘 정렬 (1차 버그 수정)**:
  - 문제: 스냅 행이 `density-inner`(라벨·카운트 포함) 폭 기준으로 배치돼 트랙보다 넓게 퍼짐. 아이콘이 트랙 우측 끝을 넘어감.
  - 해결: 스냅 행을 **트랙과 정확히 같은 폭·같은 좌우 오프셋**에 정렬. 즉 스냅 행의 좌우 경계 = 트랙의 좌우 경계. `colsToPos()` 계산의 기준 폭을 `density-inner`가 아니라 **트랙 폭**으로 통일.
  - 구현: 스냅 행을 트랙과 같은 컨테이너(라벨·카운트 제외 영역)에 넣거나, 트랙과 동일한 left/right margin을 갖게 한다. knob과 스냅 아이콘이 **같은 좌표계**(트랙 기준 colsToPos)를 쓰도록.
  - 각 스냅 스텝: `position:absolute; left:${colsToPos(c)*100}%; transform:translateX(-50%)` — 단 이 %는 **트랙 폭 기준**이어야 함.
- 스냅 아이콘 = 박스 N개만(숫자 없음). 활성 열 박스는 흰색.
- 바 늘리기 금지 — 아이콘이 바 안에 정확히 들어오게만.

---

## 5. 카드 하단 텍스트 / 호버

- 타이틀 상시 노출. 호버 시 요약(타이폴로지 · AWARDS) 하단 예약 공간에 페이드인. 전 구간 노출(조밀 구간도 숨기지 않고 텍스트만 축소).
- 카드에 `cursor: pointer` (1차 누락). 링월 ProjectCard는 Link라 자동이나 그리드는 명시 필요.

---

## 6. 반응형 종횡비 상한

- 밀도 상한 = 뷰포트 종횡비: `r<0.85`→2, `0.85≤r<1.25`→4, `r≥1.25`→6.
- 상한 변경 시 현재 cols가 상한 초과면 클램프.

---

## 7. 검증 (영상 판독 불필요 — 좌표 측정으로)

- `npx tsc --noEmit`만. (dev/build 금지)
- 신규 2파일 외 diff 없음 확인.
- **위치 고정 검증**: 전환 중(t=0.5 등) 기존 카드의 x좌표가 A·B에서 col이 동일한지, opacity=1 유지되는지 좌표 측정.
- **균등폭 검증**: 각 정수 열에서 모든 카드 width 동일(1px 오차 내).
- **슬라이더 정렬 검증**: 마지막 스냅 아이콘의 중심 x = 트랙 우측 끝 x, 첫 아이콘 = 트랙 좌측 끝.

---

## 실행 프롬프트

```
GRID_MODE_V2_SPEC.md 파일을 읽고 명세대로 구현해줘. 기존 /work-grid 구현을 전면 교체한다.

절대 제약:
- src/app/page.tsx, work/page.tsx, LandingExperience.tsx, ContentArea.tsx, ProjectWall.tsx, MobileProjectWall.tsx, useRingWall.ts 절대 수정 금지. 신규 2파일(work-grid/page.tsx, GridExperience.tsx)만.
- 카드는 항상 4:3 균등폭. 원본 aspectRatio를 카드폭에 반영하지 마. object-fit:cover + coverHotspot으로 크롭.
- 밀도 전환 핵심: 화면에 보이는 카드는 절대 위치 이동 금지. col=i%ncol 방식 쓰지 마. 대신 정수열 A→B 레이아웃을 각각 계산하고, B의 앞부분은 A와 동일 (row,col) 유지, 새로 생긴 열 슬롯만 대기카드로 opacity 0→1 페이드. 기존 카드는 폭만 A→B 보간, 위치 고정.
- 절대좌표 transform translate(px 기반, 퍼센트 금지) 방식.
- 슬라이더 스냅 아이콘을 트랙과 같은 폭·좌표계에 정렬(바 늘리지 말고 아이콘을 트랙 안에). knob과 스냅이 같은 colsToPos(트랙폭 기준) 사용.
- 덴시티바 width:min(440px,64vw). 카드 cursor:pointer. 호버 요약 전구간 노출.
- 1열 폭은 (4/3)*(vpH*0.72) 히어로폭 중앙정렬. MIN_COLS 한곳 정의.
- 완료 후 npx tsc --noEmit만.
```
